## Phase 1
- [x] マイグレーションの作成と実行
  - [x] `cities` テーブルの作成
  - [x] `characters` テーブルへ `current_city_id`, `highest_city_id` 追加
  - [x] `areas` テーブルへ `city_id` 追加
- [x] モデルの作成と更新
  - [x] `City` モデル作成
  - [x] `Character` モデルのリレーション追加
  - [x] `Area` モデルのリレーション追加
- [x] Seederの作成と実行
  - [x] `CitySeeder` の作成（4都市）
  - [x] 既存の `areas` と `characters` に初期街を割り当てる処理（Seederまたはマイグレーション内）
- [x] 探索画面 (`ExploreController`等) の改修
  - [x] 現在地の街に紐付くエリアのみ表示
  - [x] 現在地の街名を表示
- [x] 街移動機能の作成
  - [x] `/city` ルートとコントローラー作成
  - [x] 街一覧画面のビュー作成（解放済みのみ移動可能）
  - [x] 移動ロジック（`current_city_id` の更新）

## Phase 2
- [x] CitySeederの更新（全10都市の追加）
  1. 王都アークレア
  2. 港町マリネス
  3. 精霊都市エルフィア
  4. 鉄鋼都市グランベルグ
  5. 雪都フロストリア
  6. 砂都サンドラ
  7. 魔導都市ルミナス
  8. 魔界都市ネクロム
  9. 天空都市セレスティア
  10. 魔王城ヴァルゼリア
- [x] 街の解放ロジックの実装
  - [x] 最終エリア（ボス）クリア時に `highest_city_id` をインクリメント（解放）する処理（`AreaService@unlockNextArea` 付近）
- [ ] 職業解放との連携
  - [ ] 転職所の表示・転職可能条件に「特定の街の到達」を追加（要件に合わせて後日詳細化）

## Phase 3
- [x] 世界地図画像の表示
  - [x] 街移動画面の上に、いただいた世界地図の画像を配置
  - [ ] 世界地図の上に現在地ピンなどをCSSでマッピングできれば理想（今回は簡単な画像表示のみ）
