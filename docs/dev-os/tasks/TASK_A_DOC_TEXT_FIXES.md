# 実装指示書: 安全な文言・ドキュメント修正一括タスク（依頼書A）

Status: 未着手（人間確認後にCodexへ渡す）

## 目的
2026-07-02の仕様監査（docs/dev-os/SPEC_AUDIT_2026-07-02.md）で見つかったドキュメント不備と、プレイヤー可視の旧表記（MP/STR/AGI）を修正し、AIエージェントとプレイヤーの両方が誤った情報を見ない状態にする。

## 背景
- 正表記の裁定（2026-07-02）: プレイヤー向け表示・新規ドキュメントは HP / SP / ATK / DEF / MAG / SPR / SPD / LUK。MP / STR / AGI は旧表記。DBカラム（mp_base, str, agi 等）はリネームしない。
- 参照: AGENTS.md「Source of truth」「Fixed rulings」、docs/dev-os/SPEC_AUDIT_2026-07-02.md のB-4〜B-6。

## 現状の問題
1. docs/DOMAIN_RULES.md の Economy節冒頭3行が `<short>` プレースホルダのまま。
2. .claude/CLAUDE.md に都市名の誤記「マリナス」（正: 港町マリネス）と、エリアID記述に街道75〜83の欠落がある。
3. docs/base.md に初期設計の失効数値（レベル上限50等）が注記なしで残り、CLAUDE.mdから「原典」として参照されている。
4. config/valzeria_guide.php:20 などプレイヤー可視文言に「MP」が残る。
5. プレイヤー可視のMP/STR/AGI残存箇所が未調査。

## 実装対象
1. **docs/DOMAIN_RULES.md** — Economy節冒頭の3行を以下で置換:
   - `- Gold: 通常プレイで流通する基本通貨。主な供給源は素材・装備・換金品の売却、NPC調達納品報酬、低確率の戦闘Gold（通常敵5%/ボス12%抽選）。主な消費先は鍛冶強化、装備進化、素材交換、倉庫拡張、ショップ/市場購入、宿代、敗北時ロスト（所持Goldの10%）。`
   - `- 輝石: 課金/支援通貨。有償/無償を区別して管理する。Goldとは役割を分け、Gold→輝石の変換は導入しない。探索力回復は輝石専売。`
   - `- Paid/free distinction: 有償輝石と無償輝石は別残高として管理し、合計輝石と併せて整合を保つ。輝石ドロップは無償輝石。`
   - バランス数値は上記の転記のみとし、新しい数値を発明しない。
2. **.claude/CLAUDE.md** — ①「マリナス」→「マリネス」（都市一覧の表記。正式名は「港町マリネス」）②エリア構成の行を「Area ID 1〜70が通常ダンジョン、71〜74が特殊、75〜83が街道」に更新 ③docs/base.mdの紹介文言を「ゲーム仕様書（設計思想の原典。数値は失効しており現行正仕様はDOMAIN_RULES.md）」に変更。
3. **docs/base.md** — 冒頭（タイトル直下）に失効ノートを追加: 「この文書は初期設計の歴史的資料。レベル上限50などの数値・仕様は現行と異なる。現行正仕様は docs/DOMAIN_RULES.md と AGENTS.md の Source of truth に従うこと。」本文は変更しない。
4. **config/valzeria_guide.php** — プレイヤー可視の本文文字列内の「MP」を「SP」に修正（例: 20行目「HPやMPが減ったら」→「HPやSPが減ったら」）。**修正は表示文言のみ**。配列キー・PHP構造・その他の文言は変更しない。
5. **MP/STR/AGI残存grep調査** — `resources/views/`（Blade）、`config/`（プレイヤー向け文言を含むconfig）、`app/`内のユーザー向けメッセージ文字列を対象に、プレイヤー可視の「MP」「STR」「AGI」表記を全grepし、①今回修正した箇所②残存箇所一覧（ファイル:行、文脈、表示側かコード内部か）をレポートにまとめる。**表示文言のみ**修正し、判断に迷う箇所（変数名と表示が混在等）は修正せず一覧に「要確認」と記載する。

## 実装対象外（重要）
- コードロジック変更、変数名・カラム名・配列キーのリネーム
- DB変更、migration作成、Seeder変更
- 課金処理、デプロイ、コミット
- Blade内のPHP変数名（`$mp` 等）の変更 — 表示される文字列リテラルのみが対象
- valzeria_guide.php のMP以外の文言調整（Gold関連文言は現行実装と整合するため触らない）
- base.md 本文の書き換え（ノート追加のみ）

## 変更範囲
- 想定ファイル: docs/DOMAIN_RULES.md、.claude/CLAUDE.md、docs/base.md、config/valzeria_guide.php、grep結果に応じたBlade/config（表示文字列のみ）
- 想定しない範囲: app/Services/*、database/*、routes/*

## 既存仕様への影響
- なし（表示・ドキュメントのみ）。ヘルプ文言は `game_texts` の管理画面上書きが優先されるため、本番で上書き済みの文言がある場合はconfig修正が即座に見えない可能性がある（報告に含める）。

## DB変更
- なし

## 管理者機能への影響
- なし（ヘルプ文言管理の初期値が変わるのみ）

## ログ・更新履歴への影響
- config/admin_update_summaries.php: 「文言の旧表記（MP等）をSPへ修正」として1件追記（category: fixed）。プレイヤー可視の修正が valzeria_guide のみで軽微な場合は追記不要と判断してよい（判断理由を報告）。
- docs同期: 本タスク自体がdocs修正。追加同期不要。

## テスト観点・確認コマンド
- `php -l config/valzeria_guide.php`（構文確認）
- `rg -n "マリナス" .claude/ docs/` → 0件になること
- `rg -n "<short>" docs/DOMAIN_RULES.md` → 0件になること
- `rg -n "MP" config/valzeria_guide.php` → 表示文言中の残存が0件（キー名等コード上の残存は可）
- ローカル起動できる場合: /help と案内所を表示し、文言崩れがないこと

## 手動確認手順
1. DOMAIN_RULES.md冒頭をレビュー（数値がGold経済レビューと一致するか）
2. /help（または案内所）で修正後文言を表示確認
3. grepレポートの「要確認」一覧を人間がレビュー

## ロールバック方針
- すべてテキスト修正のため `git checkout -- <file>` で即時復元可能

## 完了条件
- [ ] 上記5対象がすべて修正・作成済み
- [ ] 確認コマンド4件がすべて期待どおり
- [ ] MP/STR/AGI残存レポート（修正済み一覧＋要確認一覧）を提出
- [ ] コードロジック・DB・課金に一切触れていないことを報告に明記

## 更新情報サマリ案
- category: fixed / title: 「案内文言の表記を調整」 / detail: 「街の案内所などの説明文で、SPをMPと表記していた箇所を修正しました。」（プレイヤー可視修正がある場合のみ）

## 人間確認が必要なポイント
- grepレポートの「要確認」箇所の修正可否
- game_texts上書きが本番に存在する場合の扱い
